{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/filedumpPipeline//nextflow_schema.json",
  "title": "Nextflow Hive Pipeline Dispatcher",
  "description": "Nexflow process to run the hive beekeeper",
  "type": "object",
  "properties": {
    "hive_init_cmd": {
      "type": "string",
      "required": true ,
      "description": "Nextflow process for managing the Hive Beekeeper pipeline, including initialization and re-creation."
    },
    "hive_force_init": {
      "type": "number",
      "default": 0,
      "minimum": 0,
      "maximum": 1,
      "description": "A flag that determines whether to forcefully delete and recreate the Hive pipeline. Set to 1 to enable, 0 to disable."
    }
  }
}

language: perl

perl:
- '5.14'

env:
- COVERALLS=true  DB=mysql

sudo: false

addons:
  apt:
    packages:
    - unzip
    - sendmail
    - graphviz
    - emboss

before_install:
- export BRANCH=$(if [ "$TRAVIS_PULL_REQUEST" == "false" ]; then echo $TRAVIS_BRANCH; else echo $TRAVIS_PULL_REQUEST_BRANCH; fi)
- $(if [git ls-remote https://github.com/Ensembl/ensembl-test.git refs/heads/$BRANCH]; then git clone --branch $BRANCH --depth 1 https://github.com/Ensembl/ensembl-test.git; else git clone --branch master --depth 1 https://github.com/Ensembl/ensembl-test.git;fi)
- $(if [git ls-remote https://github.com/Ensembl/ensembl.git refs/heads/$BRANCH]; then git clone --branch $BRANCH --depth 1 https://github.com/Ensembl/ensembl.git; else git clone --branch master --depth 1 https://github.com/Ensembl/ensembl.git;fi)
- git clone --branch master --depth 1 https://github.com/Ensembl/ensembl-hive.git
- $(if [git ls-remote https://github.com/Ensembl/ensembl-compara.git refs/heads/$BRANCH]; then git clone --branch $BRANCH --depth 1 https://github.com/Ensembl/ensembl-compara.git; else git clone --branch master --depth 1 https://github.com/Ensembl/ensembl-compara.git;fi)
- $(if [git ls-remote https://github.com/Ensembl/ensembl-datacheck.git refs/heads/$BRANCH]; then git clone --branch $BRANCH --depth 1 https://github.com/Ensembl/ensembl-datacheck.git; else git clone --branch master --depth 1 https://github.com/Ensembl/ensembl-datacheck.git;fi)
- $(if [git ls-remote https://github.com/Ensembl/ensembl-variation.git refs/heads/$BRANCH]; then git clone --branch $BRANCH --depth 1 https://github.com/Ensembl/ensembl-variation.git; else git clone --branch master --depth 1 https://github.com/Ensembl/ensembl-variation.git;fi)
- $(if [git ls-remote https://github.com/Ensembl/ensembl-orm.git refs/heads/$BRANCH]; then git clone --branch $BRANCH --depth 1 https://github.com/Ensembl/ensembl-orm.git; else git clone --branch master --depth 1 https://github.com/Ensembl/ensembl-orm.git;fi)
- $(if [git ls-remote https://github.com/Ensembl/ensembl-taxonomy.git refs/heads/$BRANCH]; then git clone --branch $BRANCH --depth 1 https://github.com/Ensembl/ensembl-taxonomy.git; else git clone --branch master --depth 1 https://github.com/Ensembl/ensembl-taxonomy.git;fi)
- $(if [git ls-remote https://github.com/Ensembl/ensembl-metadata.git refs/heads/$BRANCH]; then git clone --branch $BRANCH --depth 1 https://github.com/Ensembl/ensembl-metadata.git; else git clone --branch master --depth 1 https://github.com/Ensembl/ensembl-metadata.git;fi)
- $(if [git ls-remote https://github.com/Ensembl/ensembl-funcgen.git refs/heads/$BRANCH]; then git clone --branch $BRANCH --depth 1 https://github.com/Ensembl/ensembl-funcgen.git; else git clone --branch master --depth 1 https://github.com/Ensembl/ensembl-funcgen.git;fi)
- git clone --branch develop --depth 1 https://github.com/samtools/htslib.git
- cd htslib
- make
- export HTSLIB_DIR=$(pwd -P)
- cd ..
- git clone --branch release-1-6-924 --depth 1 https://github.com/bioperl/bioperl-live.git

install:
- cpanm -v --installdeps --with-recommends --notest --cpanfile ensembl/cpanfile .
- cpanm -v --installdeps --notest --cpanfile ensembl-hive/cpanfile .
- export PERL5LIB=$PERL5LIB:$PWD/bioperl-live
- cpanm -v --installdeps --notest .
- cpanm -n Devel::Cover::Report::Coveralls
- cp travisci/MultiTestDB.conf.travisci  modules/t/MultiTestDB.conf
- mysql -u root -h localhost -e 'GRANT ALL PRIVILEGES ON *.* TO "travis"@"%"'

script: ./travisci/harness.sh

notifications:
  email:
    on_success: always
    on_failure: always
  slack:
    secure: BkrSPAkOM5aTOpeyO9vZnHdZ0LF1PLk0r2HtcXN2eTMyiHoGXkl6VUjdAL8EkzI4gunW2GProdSIjHpf60WdiEmKAulMdJRI+xyUbuxnY31mwiikS9HYwqmPBbMTf0Mh2pMBngZRFs+gaFZDUMTfLfp+8MQfU1R54yb6hPuVt5I=
